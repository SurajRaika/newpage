---
const buildTime = new Date();
const formattedBuildTime = new Intl.DateTimeFormat("en-US", {
  dateStyle: "medium",
  timeStyle: "short",
}).format(buildTime);
---



<div class="daily-progress-container flex-1 max-w-[30%]   flex-col gap-2 w-full pl-1 sm:w-auto sm:pl-0">
    <div class="progress-label text-xs py-2 font-medium text-[rgb(var(--color-text-secondary))] uppercase
                dark:text-[rgb(var(--color-text-secondary))]">
        DAY REMAINING:<span id="day-remaining-percent">0.0</span>%
    </div>
    <div class="progress-bar-wrapper bg-[rgb(var(--color-border-color))] rounded-xs h-2 w-full overflow-hidden
                dark:bg-[rgb(var(--color-border-color))]">
        <div id="day-progress-bar" class="progress-bar  bg-[rgb(var(--color-accent))] h-full w-0 rounded-xs
                    dark:bg-[rgb(var(--color-accent))] transition-all duration-1000 ease-out"></div>
    </div>
</div>






<div class="fixed top-0 left-0 z-50 text-xs text-gray-400 opacity-50 px-2 py-1 pointer-events-none select-none">
  Built Time:{formattedBuildTime}
</div>


<style>
    #day-remaining-percent {
        transition: all 0.5s ease-out;
        display: inline-block;
        min-width: 4.5ch; /* Enough space for "100.0" */
        text-align: right; /* Right-align the numbers for consistent positioning */
        font-variant-numeric: tabular-nums; /* Use monospace numbers */
    }
    
    .progress-bar {
        transform-origin: left;
        will-change: width, transform;
    }
    
    /* Ensure smooth rendering and prevent layout shifts */
    .daily-progress-container {
        contain: layout style;
        flex-shrink: 0; /* Prevent the container from shrinking */
        width: 150px; /* Fixed width to prevent parent layout shifts */
    }
    
    .progress-label {
        white-space: nowrap; /* Prevent text wrapping */
    }
</style>

    <script is:inline src="/scripts/dailyProgressBar.js"></script>